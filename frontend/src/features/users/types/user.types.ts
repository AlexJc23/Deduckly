export type MonthlyGoal = {
    goal: number;
    current: number;
    remaining: number;
    over_goal: number;
    progress: number;
    percentage: number
}

export type DailyGoal = {
    goal: number;
    current: number;
    remaining: number;
    over_goal: number;
    progress: number;
    percentage: number;
}